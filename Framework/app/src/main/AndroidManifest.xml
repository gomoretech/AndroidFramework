<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.Android.com/tools"
    package="com.gomore.framework">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/palmmall_launcher_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon, android:theme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".pickerview.FragmentTestActivity" />
        <activity android:name=".pickerview.JsonDataActivity" />

        <activity
            android:name=".SampleActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".HomeActivity">

        </activity>


        <service
            android:name=".upload.DemoService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".UpdateActivity">

        </activity>
        <activity
            android:name=".upload.CustomVersionDialogActivity"
            android:theme="@style/versionCheckTransparentTheme"></activity>
    </application>

</manifest>